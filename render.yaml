services:
  - type: web
    name: pranker-server
    env: rust
    region: singapore
    plan: free
    buildCommand: cargo build --release --bin pranker-server
    startCommand: ./target/release/pranker-server
    envVars:
      - key: RUST_LOG
        value: info
